# Central Storage System Configuration

[DEFAULT]
base_url = http://localhost:8080
timeout = 10

[AUTH]
# Default admin credentials (change these!)
admin_username = admin
admin_password = admin123

[BATCH_OPERATIONS]
# Default batch sizes
default_page_size = 20
max_page_size = 100
batch_progress_interval = 10
